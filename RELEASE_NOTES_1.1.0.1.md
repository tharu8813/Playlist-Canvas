> [!CAUTION]
> 영상 요소가 포함된 프로젝트와 GPU 영상 인코딩은 그래픽 장치, 드라이버 및 FFmpeg 빌드의 지원 범위에 영향을 받습니다.
> 내보내기 시작 전 사전 검사를 실행하며, 선택한 GPU 인코더를 시작할 수 없으면 CPU H.264를 선택해 주세요.
>
> 고해상도 영상은 첫 미리보기에서 저화질 프록시를 준비할 수 있습니다. 준비 중에는 포스터 프레임이 표시되며, 완성된 프록시는 이후 미리보기에서 재사용됩니다. 최종 내보내기에는 항상 원본 영상을 사용합니다.
>
> 1.1.0.1에서 저장한 프로젝트에 영상 요소나 곡별 영상이 포함되어 있으면 이전 버전에서 동일하게 표시되지 않을 수 있습니다. 중요한 프로젝트는 업데이트 전에 백업해 주세요.

# Playlist Canvas 1.1.0.1

1.1.0.0 이후 영상 요소, GPU 기반 통합 미리보기, 저화질 프록시와 스트리밍 내보내기를 추가하고 편집·인코딩 안정성과 성능을 전반적으로 개선한 업데이트입니다.

## 주요 변경 사항

- 이미지 프레임을 대량으로 저장하던 기존 내보내기를 제한된 메모리 큐와 FFmpeg 스트리밍 방식으로 전환
- 캔버스 영상 요소와 곡별 영상 등록 기능 추가
- 전체 타임라인·곡별 실행 범위와 1회·단일 반복·순차 반복·랜덤 반복 방식 지원
- 영상 속도, 채우기 방식, 둥근 모서리, 블러와 색상 효과 설정 추가
- 편집 캔버스에서는 영상 요소를 포스터로 표시하고 통합 미리보기에서만 실제 영상을 재생하도록 변경
- 하단에 미리보기 탭을 추가하고 선택 시 캔버스는 재생 화면, 하단은 재생 컨트롤, 오른쪽 속성 영역은 트랙 목록으로 전환하도록 변경
- GPU 레이어 미리보기를 기본 렌더러로 적용하고 설정에서 다음 실행에 사용할 렌더러를 선택하도록 변경
- 미리보기 렌더러는 설정에서만 변경할 수 있도록 정리하고 변경 사항이 다음 프로그램 실행부터 적용됨을 안내
- 현재 트랙 하이라이트와 트랙 더블클릭 이동, 상단 성능 정보 및 확장 가능한 오류 안내 추가
- GPU 미리보기의 적응형 렌더 배율, 텍스처 캐시, 프레임 대기 제한과 자동 CPU 대체 처리 추가
- 프로젝트 콘텐츠 더블클릭 시 종류별 미리보기와 파일 정보를 표시하고 드래그 앤 드롭으로 캔버스에 추가하도록 변경
- 왼쪽의 요소·프로젝트 콘텐츠·레이어를 하나의 탭 패널로 통합
- 보기 메뉴와 단축키로 왼쪽·오른쪽·하단 작업 패널을 각각 표시하거나 숨길 수 있도록 개선
- 왼쪽·오른쪽·하단 패널의 크기와 표시 상태를 프로그램 재실행 후에도 유지하도록 개선
- 요소 속성 화면의 영상 범위·반복 설정과 곡 설정의 영상 순서 편집을 정리
- 요소 등장·퇴장 효과를 통합하고 이동 애니메이션에도 자연스러운 페이드 인·아웃 적용
- 팝과 회전 등장·퇴장 애니메이션 추가
- 내보내기 설정 창을 두 열 구조로 정리하고 출력·렌더링·화질·고급 설정의 구분 개선
- 내보내기 사전 검사, 단계별 진행률, 경과·예상 시간 계산과 취소·종료 흐름 강화

## 미리보기 성능 및 안정성

- 960×540 또는 30FPS를 초과하는 영상에 미리보기 전용 프록시를 백그라운드에서 생성하도록 추가
- 원본 영상의 경로·크기·수정 시각을 기준으로 프록시를 재사용하고 캐시를 최대 2GB로 제한
- 프록시 준비 중에는 고해상도 원본을 동시에 디코딩하지 않고 포스터를 표시한 뒤 현재 타임코드에서 자동 전환
- 960×540·30FPS 이하 영상은 변환 없이 사용하고 24FPS 원본은 프록시에서도 같은 FPS를 유지
- 투명 채널이 있는 영상은 미리보기 투명도를 보존하기 위해 프록시 변환에서 제외
- 프록시 생성에 실패하면 작업을 중단하지 않고 원본 영상 재생으로 복귀하며 성능 영역과 경고 배너에 상태를 표시
- 프록시 생성 프로세스를 낮은 우선순위와 제한된 CPU 스레드로 실행하고 미리보기 종료 시 부분 파일과 프로세스를 정리
- 영상 프레임을 별도 GPU 레이어로 합성하고 지원되는 GPU 프레임은 `toImage()` 변환 없이 유지하도록 개선
- NV12·NV21 영상 프레임을 RGB 이미지로 변환하지 않고 GPU 평면 텍스처와 셰이더로 표시하도록 개선
- 영상 위에 다른 요소가 있어도 전체 캔버스를 매 프레임 다시 캡처하지 않도록 정적 Z 구간을 캐시
- 정상 재생 중 반복되는 강제 탐색을 제한하고 실제 타임라인 이동과 일시정지 탐색만 즉시 반영하도록 개선
- 디코더 부하에 따라 영상 해상도와 FPS를 서로 독립적으로 낮추고 안정되면 단계적으로 복구하는 백프레셔 추가
- 새 영상 프레임이 도착하지 않은 재생 틱과 완전히 정적인 장면의 중복 이미지 복사·GPU 제출 제거
- 요소 목록, Z 순서, 애니메이션 구간과 비주얼라이저 활성 상태를 캐시하여 매 프레임 반복 계산 감소
- 긴 플레이리스트의 현재 곡 탐색을 선형 탐색에서 이진 탐색으로 변경

## 수정 사항

- 장시간 영상에서 임시 PNG 프레임이 대량 생성되어 저장 공간을 과도하게 사용하던 문제 수정
- 정적 캔버스 영상을 다시 인코딩하던 불필요한 단계를 제거하고 가능한 경우 최종 코덱으로 직접 인코딩하도록 개선
- 서로 독립적인 영상 정보를 병렬로 검사하고 반복 입력은 하나의 물리적 FFmpeg 입력을 공유하도록 개선
- 출력 드라이브가 작업 드라이브와 다를 때의 여유 공간 검사와 최종 파일 이동 안정성 개선
- AMF 등 하드웨어 인코더가 설치되어 있어도 실제 설정으로 시작할 수 없는 경우를 사전 검사에서 판별하도록 수정
- 내보내기 실패 시 기존 출력 파일을 보존하고 임시 스트림과 디코더를 정리하도록 수정
- 준비 단계와 최종 인코딩 도중 취소하거나 프로그램을 종료할 때 작업이 남을 수 있던 문제 수정
- 회전한 요소의 모서리 투명도와 영상의 크기·채우기·색상 효과가 미리보기와 결과물에서 달라지던 문제 수정
- 곡 사이 공백, 잘린 영상 구간과 플레이리스트 끝에서 영상 타임라인이 어긋나던 문제 수정
- 동적 요소와 정적 요소의 Z 순서가 미리보기 또는 내보내기에서 달라질 수 있던 문제 수정
- 다음 곡으로 이동할 때 영상이 잠시 멈추거나 일시정지 상태에서 영상 프레임이 갱신되지 않던 문제 수정
- 영상 원본의 해상도와 FPS가 높을수록 미리보기 프레임 드롭과 UI 끊김이 심해지던 문제를 프록시 재생과 디코더 백프레셔로 개선
- FFprobe가 미리보기 UI를 막던 문제를 비동기 처리하고 현재 곡과 다음 곡의 영상 정보를 미리 읽도록 개선
- 사용하지 않는 영상 디코더의 반복 명령과 자원 점유를 줄이고 종료 시 안전하게 해제하도록 개선
- 비주얼라이저 병렬 작업의 진행 문구가 번갈아 표시되거나 캐시 인덱스가 잘못 대응하던 문제 수정
- GPU 프레임 지연, 컨텍스트 손실과 필터 대체 처리에서 오류가 중복 표시되거나 미리보기가 멈출 수 있던 문제 수정
- GPU 미리보기 시작 직후 비주얼라이저 프레임이 준비되지 않아 경고가 표시되던 문제와 AMD OpenGL 텍스처 갱신 경고 수정
- 프로젝트 콘텐츠를 캔버스로 드래그해도 추가되지 않던 문제 수정
- 미리보기 전환과 수동 패널 표시·숨김에서 왼쪽·오른쪽·하단 패널의 애니메이션과 상태가 서로 달라지던 문제 수정
- 내보내기 단계가 바뀔 때 남은 시간이 크게 튀거나 정지해 보이던 문제 개선

## 설치

`Playlist Canvas-1.1.0.1-setup.exe`를 내려받아 실행하세요. 기존 설치본이 있으면 같은 설치 위치에 업데이트됩니다.

FFmpeg는 프로그램에 포함되지 않으며 **도구 → 설정 → FFmpeg → FFmpeg 자동 다운로드 및 설치**에서 검증된 배포본을 설치할 수 있습니다.

> 코드 서명이 적용되지 않은 설치 파일은 Windows SmartScreen 안내가 표시될 수 있습니다.
