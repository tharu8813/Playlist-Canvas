-r requirements.txt
pyinstaller==6.22.0

# AutoMix's structure analyzer ships in the Windows installer (the spec refuses
# to build without it; see PACKAGING.md). It is a ~2 MB Rust extension. The
# PyTorch-based analyzers (Beat This!, Demucs) are deliberately NOT shipped:
# the light analyzer is the default, and the spec excludes them.
sonara==0.3.6
